---
import BaseLayout from '../layouts/BaseLayout.astro';
const allPosts = await Astro.glob('../pages/posts/*.md');
import BlogPost from '../components/BlogPost.astro';
const pageTitle = "Mi blog de Astro aprendizaje" ;
---


<BaseLayout pageTitle={pageTitle}>
  <h2>Mi impresionante subtítulo del blog</h2>
  <p> Bienvenidos a Mi Blog. </p>
   <p> Aquí les hablaré de lo que  iré desarrollando día a día, de acuerdo a los avances que daré  con Astro</p>

   <ul class="culo">
     {allPosts.map((post) => <BlogPost url={post.url} title={post.frontmatter.title} />)}
   </ul>
</BaseLayout>

